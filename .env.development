# see config/config.ts for application layer naming and usage of vars
API_URL="http://127.0.0.1:8000"

ACROSS_API_TOKEN="local-development-frontend-token"

# build time static public env vars must have a PUBLIC_ prefix
PUBLIC_BUILD_VERSION="local-dev"
